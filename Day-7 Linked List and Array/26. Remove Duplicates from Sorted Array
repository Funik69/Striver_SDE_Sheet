class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        vector<int> v;
        for(auto i:nums){
            if(v.size() && v.back()==i)
            continue;
            else
            v.push_back(i);
        }
    return v.size();}
};
